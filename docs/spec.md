# ネットワークシミュレータ 仕様書 v3.3

---

## 1. 概念定義

### ノード（5種類）

| 種別 | 役割 | ポート数 | 設定できる項目 |
|---|---|---|---|
| Router | 異なるネットワーク間の転送 | 8 | 論理IF・IPアドレス・スタティックルーティング |
| Switch | 同一ネットワーク内の転送 | 8 | IPアドレス・VLAN |
| PC | Ping送信元・Webブラウザ送信元 | 1 | IPアドレス・サブネットマスク・デフォルトGW・DNSサーバ |
| Server | Ping応答元・Webサーバ | 8 | IPアドレス・サブネットマスク・デフォルトGW・DNSサーバ・ホストドメイン |
| DNSサーバ | ドメイン名前解決 | 8 | IPアドレス・サブネットマスク・デフォルトGW・Aレコード |

### ポート

| 種別 | ポート名の形式 | 本数 |
|---|---|---|
| Router | GigabitEthernet0/0 〜 GigabitEthernet0/7 | 8 |
| Switch | GigabitEthernet0/0 〜 GigabitEthernet0/7 | 8 |
| PC | eth0 | 1 |
| Server | eth0 〜 eth7 | 8 |
| DNSサーバ | eth0 〜 eth7 | 8 |

### リンク
- 2つのノードの各ポートを指定して繋ぐ線
- 同じノード間に1本のみ存在可能（重複リンク禁止）
- 自己ループ禁止（同じノードのポート同士は繋げない）
- 1ポートに1リンクのみ接続可能

---

## 2. 制約

- 物理IFの装脱着は行わない（RouterのIFは論理IFのみ）
- 重複リンク禁止
- 自己ループ禁止
- 1ポートに1リンクのみ
- ノード削除時は紐づくリンクも同時に削除され、ポートが空きに戻る
- DNSのAレコードは1ドメインにつき1レコードのみ

---

## 3. 画面構成・遷移

```
トップ画面（/）
  ├── シミュレーションモードへ  →  /simulation
  └── 演習モードへ             →  /exercises

演習一覧画面（/exercises）
  ├── 演習1カードをクリック    →  /exercises/1
  └── 演習2カードをクリック    →  /exercises/2
```

- 全ページの左上にアプリタイトルを表示する
- タイトルをクリックするとトップページ（/）に戻る
- ホバー時にカーソルが pointer になる

---

## 4. トップ画面（/）

- アプリ名・説明文を表示
- 「シミュレーションモードへ」ボタン → /simulation へ遷移
- 「演習モードへ」ボタン → /exercises へ遷移

---

## 5. シミュレーションモード（/simulation）

自由にノードを配置・設定・通信できるモード。

### 5-1. GUI全体レイアウト

```
┌──────────┬────────────────────────────────────┬──────────────┐
│  ノード  │  [タイトル]        [シミュトグル]  │  設定パネル  │
│ パレット ├────────────────────────────────────┤  （右側）    │
│  （左）  │     キャンバス                     │              │
│          ├────────────────────────────────────┤              │
│          │  操作バー・ログ                    │              │
└──────────┴────────────────────────────────────┴──────────────┘
```

### 5-2. ノードアイコン

| 種別 | react-icons |
|---|---|
| Router | MdRouter |
| Switch | MdDeviceHub |
| PC | MdComputer |
| Server | MdStorage |
| DNSサーバ | MdPublic |

### 5-3. ノード操作

- **配置**: 左パレットからキャンバスへドラッグ＆ドロップ
- **移動**: キャンバス上のノードをドラッグ
- **初期名**: 種別ごとに自動採番（Router1, Router2 / Switch1 / PC1 / Server1 / DNS1）
- **設定を開く**: ノードを右クリック →「設定」→ 右パネルに表示
- **削除**: ノードを右クリック →「削除」

### 5-4. リンク操作

**作成フロー（2ステップポート選択）:**
1. ノードをクリックすると空きポート一覧がポップアップで表示される
2. ポップアップからポートを選択すると接続元が確定し、仮線が表示される
3. 接続先ノードをクリックすると空きポート一覧がポップアップで表示される
4. 接続先ポートを選択するとリンクが作成される
5. ESCキーまたはキャンバス背景クリックでキャンセル

**選択・削除・再接続:**
- リンクをクリックすると選択状態になり黄色にハイライトされる
- 選択中にDeleteキーを押すとリンクが削除される
- リンクの端点（ノード側の末端）をドラッグすると対象ポートのリンクが切断され
  別ノードへの再接続モードに入る
- キャンバス背景をクリックするとリンクの選択が解除される
- リンクを右クリック →「削除」でも削除可能

### 5-5. ノード設定パネル（右側）

**Router設定パネル**
- ポート一覧（GigabitEthernet0/0〜0/7）・接続状態（空き/使用中）表示
- 接続中ポートのIPアドレス・サブネットマスク入力
- Ciscoコンフィグ入力エリア（CLI直打ち）
  - 対応コマンド: `interface` / `ip address` / `ip route` / `no shutdown`
- Ping送信ボタン（宛先IP入力欄つき）

**Switch設定パネル**
- ポート一覧（GigabitEthernet0/0〜0/7）・接続状態表示
- IPアドレス・サブネットマスク入力欄
- Ciscoコンフィグ入力エリア（CLI直打ち）
  - 対応コマンド: `interface` / `ip address` / `vlan` / `switchport`
- VLAN一覧表示

**PC設定パネル**
- eth0の接続状態表示
- IPアドレス・サブネットマスク・デフォルトGW・DNSサーバ入力欄
- Ping送信ボタン（宛先IP or ホスト名入力欄つき）
- Webアクセスボタン（URL入力欄つき）

**Server設定パネル**
- ポート一覧（eth0〜eth7）・接続状態表示
- IPアドレス・サブネットマスク・デフォルトGW・DNSサーバ入力欄
- ホストドメイン一覧（追加・削除可能）

**DNSサーバ設定パネル**
- ポート一覧（eth0〜eth7）・接続状態表示
- IPアドレス・サブネットマスク・デフォルトGW入力欄
- Aレコード一覧（ドメイン名・IPアドレスの追加・削除）

### 5-6. シミュレーションモードトグル

- 画面右上にシミュレーションモードのトグルボタンを配置する

**ONのとき（ステップ実行モード）:**
- スタートボタンを押すと通信が開始される
- 各ノード間ごとに一時停止し「次へ」ボタンが有効になる
- 「次へ」を押すまで現在ホップのアニメーションをループ再生する
- 「次へ」を押すと次のホップへ進む
- 往復通信を可視化する（送信方向→受信方向の順に全ホップ表示）

**OFFのとき（即時確認モード）:**
- 疎通確認ボタンを押すと即座に結果を表示する
- アニメーションは表示しない
- 結果のみをログエリアに表示する

### 5-7. パケットアニメーション（シミュレーションモードON時のみ）

- SVG の radialGradient を使った光点がリンクに沿って移動する
- 光点の移動は CSSアニメーション（duration: 600ms・ease-in-out）
- 「次へ」を押すまで現在ホップのアニメーションをループ再生する
- 光点が通過したリンク部分は一時的にハイライトされる（stroke変更）
- 到達したノードはグロー効果で一瞬ハイライトされる

**往復通信の表示:**
- 前半: 送信元 → 宛先 の方向（outbound）
- 後半: 宛先 → 送信元 の方向（return）
- 例: A→B→C の場合
  steps = [A→B(outbound), B→C(outbound), C→B(return), B→A(return)]

### 5-8. 操作バー（下部）

- スタート・次へ・リセットボタン
- ログエリア（各ホップの通信内容をテキスト表示・現在ホップをハイライト）

### 5-9. トポロジの保存・読み込み

- **保存**: 上部メニューから「保存」→ JSONファイルとしてダウンロード
- **読み込み**: 上部メニューから「読み込み」→ JSONファイルを選択して復元
- 読み込み前に上書き確認ダイアログを表示する
- 保存対象: ノード一覧・ポート状態・リンク一覧・設定値・座標

---

## 6. 演習モード

### 6-1. 演習一覧画面（/exercises）

- 演習カードをグリッド上に配置する
- 各カードに表示する要素:
  - 難度バッジ（初級・中級・上級）
  - タイトル
  - 演習内容の説明文
- カードをクリックすると各演習画面へ遷移する

### 6-2. 演習画面の共通仕様

**レイアウト**
```
┌─────────────────────────────────────────────────┐
│  [タイトル]  演習タイトル・説明                 │
├──────────┬──────────────────────────┬───────────┤
│  パレット │     キャンバス           │ 設定パネル│
│（制限あり）│                         │           │
│          ├──────────────────────────┤           │
│          │  操作バー・Answerボタン  │           │
└──────────┴──────────────────────────┴───────────┘
```

**Answerボタン:**
- 画面下部の操作バーに配置する
- クリアの場合: 画面中央に「CLEAR!!」をオーバーレイ表示
  - 「演習一覧に戻る」ボタンを表示する
- 失敗の場合: 「NG」と失敗理由をオーバーレイ表示
  - 「もう一度」ボタンでオーバーレイを閉じて再挑戦できる

### 6-3. 演習1: VLAN制御（/exercises/1）

**難度**: 初級 / **タイトル**: VLAN制御
**説明**: 4つのPCを一つのスイッチで2組のネットワークに分離する

**初期状態:**
- PC1・PC2・PC3・PC4（正方形の四隅）・Switch1（中央）を配置済み
- ノードの追加・削除・移動は不可
- 配線とConfig設定のみ可能

**正解判定:**
1. PC1とPC2が同一VLANに属しPing疎通が取れる
2. PC3とPC4が同一VLANに属しPing疎通が取れる
3. PC1/PC2とPC3/PC4の間でPing疎通が取れない（VLAN分離できている）

### 6-4. 演習2: ルーティング設定（/exercises/2）

**難度**: 初級 / **タイトル**: ルーティング設定
**説明**: 別セグメントの2台のPCをルーターで繋いで疎通を取る

**初期状態:**
- PC1（192.168.1.1/255.255.255.0 設定済み・変更不可）
- PC2（192.168.2.1/255.255.255.0 設定済み・変更不可）
- パレットにはSwitchとRouterのみ表示
- 追加上限: Router×1・Switch×2

**正解判定:**
1. PC1とPC2の間でPing疎通が取れる
2. 経路にルーターが含まれている（直結は不正解）

---

## 7. スコープ外（未対応）

- OSPFなどのダイナミックルーティング
- NATアドレス変換
- ACL（アクセスコントロールリスト）
- IPv6
- マルチキャスト通信
- 演習の進捗保存
